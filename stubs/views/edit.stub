@extends('layouts.master')

@section('content')
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm border-0 rounded-3">
            <div class="card-header bg-white py-3 border-bottom-0 pt-4 pb-0 d-flex justify-content-between align-items-center">
                <h5 class="m-0 font-weight-bold text-dark fs-5">Edit {{modelName}}</h5>
            </div>
            <div class="card-body p-4">
                <form action="{{ route('{{modelNamePlural}}.update', ${{modelNameSingular}}->id) }}" method="POST" {{enctype}}>
                    @csrf 
                    @method('PUT')
                    
                    {{formFieldsEdit}}
                    
                    <div class="mt-4 pt-3 border-top d-flex justify-content-end gap-2">
                        <a href="{{ route('{{modelNamePlural}}.index') }}" class="btn btn-light border px-4 text-secondary">Batal</a>
                        <button type="submit" class="btn btn-primary px-4 shadow-sm">
                            <i class="bi bi-save me-1"></i> Update
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@endsection