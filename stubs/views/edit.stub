@extends('layouts.master')

@section('content')
<div class="row justify-content-center">
    <div class="col-md-8">
        <div class="card shadow-sm border-0">
            <div class="card-header bg-white py-3">
                <h5 class="m-0 font-weight-bold text-primary">Edit {{modelName}}</h5>
            </div>
            <div class="card-body">
                <form action="{{ route('{{modelNamePlural}}.update', ${{modelNameSingular}}->id) }}" method="POST">
                    @csrf @method('PUT')
                    {{formFieldsEdit}}
                    <hr>
                    <div class="d-flex justify-content-between">
                        <a href="{{ route('{{modelNamePlural}}.index') }}" class="btn btn-light text-secondary">Kembali</a>
                        <button type="submit" class="btn btn-primary px-4">Update Perubahan</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@endsection