@extends('layouts.master')

@section('content')
<div class="card shadow-sm border-0 mb-4">
    <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center border-bottom-0 pt-4 pb-0">
        <h5 class="m-0 font-weight-bold text-dark fs-4">Manajemen {{modelName}}</h5>
        <a href="{{ route('{{modelNamePlural}}.create') }}" class="btn btn-primary shadow-sm d-flex align-items-center gap-2 px-3 py-2">
            <i class="bi bi-plus-lg"></i> Tambah Data
        </a>
    </div>

    <div class="card-body">
        @if(session('success'))
            <div class="alert alert-success border-0 shadow-sm alert-dismissible fade show">
                {{ session('success') }}
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        @endif

        <div class="d-flex justify-content-between align-items-center mb-3">
            <form action="{{ route('{{modelNamePlural}}.index') }}" method="GET" class="d-flex gap-2 w-100" style="max-width: 400px;">
                <input type="text" name="search" class="form-control" placeholder="Cari data..." value="{{ request('search') }}">
                <button type="submit" class="btn btn-secondary shadow-sm"><i class="bi bi-search"></i> Cari</button>
                @if(request('search'))
                    <a href="{{ route('{{modelNamePlural}}.index') }}" class="btn btn-outline-danger shadow-sm" title="Reset Pencarian"><i class="bi bi-x-lg"></i></a>
                @endif
            </form>
        </div>

        <div class="table-responsive rounded border">
            <table class="table table-hover table-striped align-middle mb-0">
                <thead class="table-light">
                    <tr class="text-uppercase text-muted" style="font-size: 0.75rem; letter-spacing: 0.5px;">
                        <th width="50" class="ps-4">No</th>
                        {{tableHeaders}}
                        <th width="150" class="text-center pe-4">Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(${{modelNamePlural}} as $item)
                    <tr>
                        <td class="fw-bold text-secondary ps-4">{{ method_exists(${{modelNamePlural}}, 'firstItem') ? ${{modelNamePlural}}->firstItem() + $loop->index : $loop->iteration }}</td>
                        {{tableBody}}
                        <td class="text-center pe-4">
                            <div class="d-flex justify-content-center gap-2">
                                <a href="{{ route('{{modelNamePlural}}.edit', $item->id) }}" class="btn btn-sm btn-warning text-dark border-0 shadow-sm" data-bs-toggle="tooltip" title="Edit">
                                    <i class="bi bi-pencil-square"></i> Edit
                                </a>
                                <form action="{{ route('{{modelNamePlural}}.destroy', $item->id) }}" method="POST" class="d-inline" onsubmit="return confirm('Apakah Anda yakin ingin menghapus data ini?');">
                                    @csrf
                                    @method('DELETE')
                                    <button type="submit" class="btn btn-sm btn-danger border-0 shadow-sm" data-bs-toggle="tooltip" title="Hapus">
                                        <i class="bi bi-trash"></i> Hapus
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    @endforeach
                </tbody>
            </table>
        </div>
        
        <div class="mt-4 d-flex justify-content-center">
            @if(method_exists(${{modelNamePlural}}, 'links'))
                {{ ${{modelNamePlural}}->appends(request()->query())->links('pagination::bootstrap-5') }}
            @endif
        </div>
    </div>
</div>
@endsection @push('scripts')
<script>
    // Notifikasi Sukses Otomatis
    @if(session('success'))
        Swal.fire({
            icon: 'success',
            title: 'Berhasil!',
            text: '{{ session("success") }}',
            showConfirmButton: false,
            timer: 2000
        });
    @endif

    // Konfirmasi Hapus
    function confirmDelete(event, formId) {
        event.preventDefault();
        Swal.fire({
            title: 'Apakah Anda yakin?',
            text: "Data yang dihapus tidak dapat dikembalikan!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#0d6efd',
            cancelButtonColor: '#dc3545',
            confirmButtonText: 'Ya, Hapus!',
            cancelButtonText: 'Batal'
        }).then((result) => {
            if (result.isConfirmed) {
                document.getElementById(formId).submit();
            }
        });
    }
</script>
@endpush